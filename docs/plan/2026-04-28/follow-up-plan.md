# review-2026-04-26-plan 후속 구현 계획

## 기준

- 작성일: 2026-04-28
- 기준 문서: `docs/review/review-2026-04-26-plan.md`
- 관련 선행 계획: `docs/plan/2026-04-27/onboarding-implementation-01.md`
- 완료된 선행 항목: `docs/plan/2026-04-28/priority-1-mypage-preference-management-plan.md`
- 목적: 기존 계획에서 아직 남은 후속 구현 항목만 정리한다.
- 범위: planner 역할 문서화만 수행한다. frontend/backend 코드, API, DB schema 변경은 포함하지 않는다.

## 앞으로 구현할 내용

### 1순위: 책 상세 행동 추가

계획 문서의 핵심 행동인 저장하기, 읽었어요, 관심 없음은 아직 책 상세에서 직접 수행할 수 없다.

구현할 내용:

- 책 상세에 `저장하기` 버튼 추가
- 책 상세에 `읽었어요` 버튼 추가
- 책 상세에 `관심 없음` 버튼 추가
- 각 행동의 현재 상태를 책 상세와 책 카드에 표시
- 행동 결과를 마이페이지의 저장한 책, 읽은 책, 추천 반영 상태에 반영

backend 필요 API 후보:

- `POST /api/me/books/{bookId}/interactions`
- 요청 예: `{ "type": "SAVE" }`, `{ "type": "READ" }`, `{ "type": "DISMISS" }`
- 저장 취소가 필요하면 `UNSAVE` 또는 `DELETE` 방식 중 하나로 별도 결정한다.

주의 사항:

- 현재 `user_book_interactions`는 `READ`, `SAVE`, `UNSAVE` 사용 흔적이 있다.
- `관심 없음` interaction 타입은 현재 코드에서 확인되지 않으므로 DB 허용 값과 schema를 먼저 확인해야 한다.
- DB schema 변경이 필요하면 작업 전에 별도 승인이 필요하다.

상세 계획:

- `docs/plan/2026-04-28/priority-2-book-detail-interactions-plan.md`

### 2순위: 개인화 홈 추천 실제화

현재 `GET /api/me/home`은 `personalized = true`를 반환하지만 추천 책은 기본 랭킹 1위에 가깝다.

구현할 내용:

- 사용자의 관심 분야를 홈 추천 후보 가중치에 반영
- 읽은 책의 카테고리와 키워드를 추천 후보 가중치에 반영
- 저장한 책을 "나중에 읽을 후보" 신호로 반영
- 관심 없음으로 표시한 책의 카테고리/키워드 가중치 하향
- 추천 이유를 사용자 행동 기반 문장으로 생성

초기 기준:

- 협업 필터링은 제외한다.
- 콘텐츠 기반 추천으로 시작한다.
- 추천 후보가 부족하면 기존 랭킹 기반 추천으로 fallback한다.

### 3순위: 추천 이유 고도화와 추천 히스토리 저장

현재 추천 이유는 카테고리 또는 필터 이유 중심의 일반 문장이다.

구현할 내용:

- 관심 분야 기반 추천 이유
- 읽은 책 기반 추천 이유
- 저장한 책 기반 추천 이유
- 공통 키워드 기반 추천 이유
- 생성된 추천 이유를 `recommendations`에 저장하는 기준 정리
- 마이페이지 추천 히스토리에 개인화 근거를 더 구체적으로 표시

좋은 추천 이유 형식:

- "읽은 책 `조용한 투자 습관`과 투자, 습관 키워드를 공유합니다."
- "관심 분야로 선택한 경제 분야에서 최근 저장이 늘어난 교양서입니다."

### 4순위: 서비스 고유 기능 강화

계획 문서의 차별화 기능은 아직 별도 화면이나 컴포넌트로 닫히지 않았다.

구현할 내용:

- 교양 필터 리포트
  - 교양서로 분류된 이유
  - 제외 키워드가 없는 이유
  - 주요 카테고리와 키워드
  - 수험서, 문제집, 전공서 성격이 낮은 근거
- 추천 근거 카드
  - 관심 분야와의 연결
  - 읽은 책과의 연결
  - 저장한 책과의 연결
  - 최근 독서 트렌드와의 연결
- 읽을 책 결정 보조
  - 이 책을 읽기 좋은 사용자
  - 읽기 난이도
  - 예상 독서 분위기
  - 비슷한 책과의 차이

주의 사항:

- 새 데이터 필드가 필요한 항목은 먼저 현재 DB에 표현 가능한지 확인한다.
- 난이도, 분위기, 제외 근거는 데이터 출처가 불명확하면 정적 문구로 시작하지 않는다.

### 5순위: 검색 기반 책 추가

현재 온보딩의 읽은 책 입력은 backend가 제공하는 선택지 목록 기반이다.

구현할 내용:

- 읽은 책 검색 입력
- 저장한 책 검색 입력
- 검색 결과에서 읽은 책 또는 저장한 책으로 추가
- 검색 결과가 없을 때의 안내

backend 필요 API 후보:

- `GET /api/books/search?query=...`

주의 사항:

- 외부 도서 API 연동은 네트워크, API key, 데이터 저장 정책이 필요하므로 별도 범위로 분리한다.
- 우선 내부 DB 검색으로 시작한다.

### 6순위: 독서 목적 저장과 추천 반영

계획 문서에는 독서 목적 선택이 포함되어 있지만 현재 온보딩 저장 요청에는 없다.

구현할 내용:

- 독서 목적 선택 UI
- 독서 목적 저장 위치 결정
- 독서 목적 기반 추천 이유와 후보 조정

주의 사항:

- 현재 확인한 코드 기준으로 독서 목적 전용 schema는 없다.
- DB schema 변경 가능성이 높으므로 구현 전 별도 승인이 필요하다.

### 7순위: 데이터 기반 추천 확장

사용자 행동 데이터가 충분히 쌓인 뒤 진행할 항목이다.

구현할 내용:

- 행동 로그 기반 랭킹 개선
- 유사 도서 추천 개선
- 유사 사용자 추천
- 리뷰 또는 키워드 기반 감정 분석

주의 사항:

- 초기에는 데이터가 적어 추천 품질이 낮을 수 있다.
- MVP 이후 지표 수집 기준을 먼저 정해야 한다.

## 권장 구현 순서

1. 책 상세 행동 API와 기존 interaction 저장 구조 확인
2. 책 상세 `저장하기`, `읽었어요`, `관심 없음` UI/API 연결
3. 행동 상태를 책 상세, 책 카드, 마이페이지에 표시
4. 개인화 홈 추천 후보 산정 로직 구현
5. 추천 이유 응답 구조 고도화
6. 교양 필터 리포트와 추천 근거 카드 설계
7. 검색 기반 책 추가와 독서 목적 저장은 별도 계획으로 분리

## 검증 계획

문서만 변경할 때:

- 별도 빌드 검증은 필요하지 않다.
- 문서 링크와 변경 내용을 확인한다.

후속 frontend 변경 시:

```powershell
cd frontend
npm run build
```

후속 backend 변경 시:

```powershell
cd backend
.\gradlew.bat test
```

frontend/backend를 함께 변경할 때는 두 검증 명령을 모두 실행한다.

## 남은 리스크

- 책 상세 행동 추가는 frontend, backend, DB interaction 타입을 함께 건드릴 수 있다.
- `관심 없음`은 현재 코드에서 명확한 저장 타입이 보이지 않아 schema 확인이 필요하다.
- `GET /api/me/home`은 개인화 플래그만 true인 상태라 사용자 기대와 실제 추천 품질 사이에 차이가 생길 수 있다.
- 추천 이유가 추상적이면 개인화 기능이 체감되지 않는다.
- 독서 목적과 난이도/분위기 정보는 현재 데이터 모델에서 바로 표현하기 어려울 수 있다.
